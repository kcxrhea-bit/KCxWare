<Application x:Class="KCxWare.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:KCxWare"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
        <SolidColorBrush x:Key="Cyan" Color="#20E6FF" />
        <SolidColorBrush x:Key="Magenta" Color="#FF2BD6" />
        <SolidColorBrush x:Key="Panel" Color="#D9111720" />
        <LinearGradientBrush x:Key="NeonGradient" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#20E6FF" Offset="0" />
            <GradientStop Color="#A86DFF" Offset="0.5" />
            <GradientStop Color="#FF2BD6" Offset="1" />
        </LinearGradientBrush>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#F5FAFF" />
            <Setter Property="FontFamily" Value="Segoe UI" />
        </Style>
        <Style x:Key="NeonButton" TargetType="Button">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="#B5131B26" />
            <Setter Property="BorderBrush" Value="{StaticResource NeonGradient}" />
            <Setter Property="BorderThickness" Value="2" />
            <Setter Property="Padding" Value="18,12" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Frame" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Frame" Property="Background" Value="#E5222940" />
                                <Setter TargetName="Frame" Property="Effect">
                                    <Setter.Value><DropShadowEffect Color="#20E6FF" BlurRadius="18" ShadowDepth="0" Opacity="0.75" /></Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True"><Setter TargetName="Frame" Property="Opacity" Value="0.78" /></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="StatusTile" TargetType="Border">
            <Setter Property="Background" Value="#B80D131C" />
            <Setter Property="BorderBrush" Value="#4420E6FF" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="7" />
            <Setter Property="Padding" Value="12,9" />
            <Setter Property="Margin" Value="4" />
        </Style>
    </Application.Resources>
</Application>
