diff --git a/README.md b/README.md
index 7292d75..011d1d4 100644
--- a/README.md
+++ b/README.md
@@ -4,13 +4,13 @@ KCxWare is a lightweight native Windows 11 WPF control panel for switching one P
 
 ## Safety model
 
-The WPF interface runs without administrator rights. After a clear restart confirmation, it starts `KCxWare.Helper.exe` through Windows UAC. The helper records a transaction under `C:\ProgramData\KCxWare`, creates one elevated logon task, and schedules a restart. At the next logon the task invokes `apply-armed`; the helper performs the settling, cleanup, and verification inline, then removes the task only after sustained success. A manually invoked elevated `apply-armed` uses that same production path, so the task is normally absent after a successful return. KCxWare does not remain resident during gaming.
+The WPF interface runs without administrator rights. After a clear confirmation, it starts `KCxWare.Helper.exe` through Windows UAC and applies the selected mode in the current session. The helper records a transaction under `C:\ProgramData\KCxWare`, verifies the result, and refreshes the UI from authoritative state. Gaming and Programming are temporary session modes; a new Windows boot restores the recorded baseline and returns KCxWare to Normal. Legacy `KCxWare Apply Armed Mode` invocations are normalized safely and never reapply an armed Gaming or Programming mode.
 
 Gaming suppression is allowlisted and session-oriented: service startup types are never disabled. Defender, Firewall, RPC, networking, Wi-Fi, audio, NVIDIA display and recording components, Chrome, Logitech input, Gaming Services, GameInput, Easy Anti-Cheat, and BattlEye are explicitly protected. Unknown and missing services or processes, including unlisted Chrome native hosts, are not modified. KCxWare never changes Fortnite files, tampers with anti-cheat, overclocks hardware, or applies undocumented registry tweaks.
 
 ## Modes
 
-- **Gaming Mode** waits for startup activity, safely shuts down WSL, stops allowlisted services to a confirmed `STOPPED` state before terminating their remaining processes, and requires a 35-second continuously clean verification window before success. This covers WSearch's observed 30-second delayed restart action; a late WSearch restart triggers another cleanup attempt, with the existing three-attempt limit still bounding the transition. It selects AMD Ryzen High Performance (`9935e61f-1661-40c5-ae2f-8495027d5d5d`) when present. Chrome and NVIDIA Overlay/Highlights remain available. OpenRGB and RustDesk remain required cleanup targets. Windows-managed PhoneExperienceHost, CrossDeviceService, and CrossDeviceResume are stopped when present but treated as logged best-effort targets because their enabled startup/COM registrations can legitimately reactivate them; their respawn alone does not fail Gaming Mode. Windows DCOM-owned WidgetService is also excluded from failure verification. A residual `wslservice` host process is benign only when WSLService, vmcompute, and `vmmemWSL` are stopped.
+- **Gaming Mode** applies immediately in the current session, waits for startup activity, safely shuts down WSL, stops allowlisted services to a confirmed `STOPPED` state before terminating their remaining processes, and requires a 35-second continuously clean verification window before success. This covers WSearch's observed 30-second delayed restart action; a late WSearch restart triggers another cleanup attempt, with the existing three-attempt limit still bounding the transition. It selects AMD Ryzen High Performance (`9935e61f-1661-40c5-ae2f-8495027d5d5d`) when present. Chrome and NVIDIA Overlay/Highlights remain available. OpenRGB and RustDesk remain required cleanup targets. Windows-managed PhoneExperienceHost, CrossDeviceService, and CrossDeviceResume are stopped when present but treated as logged best-effort targets because their enabled startup/COM registrations can legitimately reactivate them; their respawn alone does not fail Gaming Mode. Windows DCOM-owned WidgetService is also excluded from failure verification. A residual `wslservice` host process is benign only when WSLService, vmcompute, and `vmmemWSL` are stopped.
 - **Programming Mode** restores services that KCxWare recorded as running before gaming and prefers installed AMD Ryzen High Performance, AMD Ryzen Balanced, then Windows Balanced. It does not launch every heavy development application.
 - **Normal Mode** restores only services captured as running before Gaming Mode and restores the captured pre-Gaming power plan when it is still installed, otherwise preferring AMD Ryzen Balanced (`9897998c-92de-4669-853f-b7cd3ecb2790`) then Windows Balanced (`381b4222-f694-41f0-9685-ff5bb260df2e`). KCxWare intentionally does not relaunch arbitrary applications it closed because it cannot safely reconstruct their command lines or session state.
 
diff --git a/src/KCxWare.Core/Abstractions/ISystemController.cs b/src/KCxWare.Core/Abstractions/ISystemController.cs
index 0e033f3..9bae5d2 100644
--- a/src/KCxWare.Core/Abstractions/ISystemController.cs
+++ b/src/KCxWare.Core/Abstractions/ISystemController.cs
@@ -2,6 +2,7 @@ namespace KCxWare.Core.Abstractions;
 
 public interface ISystemController
 {
+    string CurrentSessionId { get; }
     Task<string?> GetActivePowerPlanAsync(CancellationToken cancellationToken = default);
     Task<bool> PowerPlanExistsAsync(string planId, CancellationToken cancellationToken = default);
     Task SetPowerPlanAsync(string planId, CancellationToken cancellationToken = default);
diff --git a/src/KCxWare.Core/Models/ModeState.cs b/src/KCxWare.Core/Models/ModeState.cs
index 0d90ffc..c4ddc3a 100644
--- a/src/KCxWare.Core/Models/ModeState.cs
+++ b/src/KCxWare.Core/Models/ModeState.cs
@@ -12,7 +12,7 @@ public sealed record TransitionRecord(
 
 public sealed record ModeState
 {
-    public const int CurrentSchemaVersion = 1;
+    public const int CurrentSchemaVersion = 2;
 
     public int SchemaVersion { get; init; } = CurrentSchemaVersion;
     public MachineMode CurrentMode { get; init; } = MachineMode.Normal;
@@ -22,6 +22,7 @@ public sealed record ModeState
     public DateTimeOffset? LastSuccessfulTransitionUtc { get; init; }
     public TransitionRecord? Transaction { get; init; }
     public string? LastError { get; init; }
+    public string? SessionId { get; init; }
 
     public bool RebootRequired => CurrentMode is MachineMode.GamingArmed
         or MachineMode.ProgrammingArmed or MachineMode.NormalArmed;
diff --git a/src/KCxWare.Core/Orchestration/ModeOrchestrator.cs b/src/KCxWare.Core/Orchestration/ModeOrchestrator.cs
index 79d2b3c..d3b0b3f 100644
--- a/src/KCxWare.Core/Orchestration/ModeOrchestrator.cs
+++ b/src/KCxWare.Core/Orchestration/ModeOrchestrator.cs
@@ -6,9 +6,75 @@ namespace KCxWare.Core.Orchestration;
 
 public sealed class ModeOrchestrator(IStateStore stateStore, ISystemController system, Action<string>? log = null)
 {
+    public string CurrentSessionId => system.CurrentSessionId;
     public Task<ModeState> GetStateAsync(CancellationToken cancellationToken = default) =>
         stateStore.LoadAsync(cancellationToken);
 
+    public async Task<ModeState> ApplyLiveAsync(MachineMode target, CancellationToken cancellationToken = default)
+    {
+        if (target is not (MachineMode.Gaming or MachineMode.Programming or MachineMode.Normal))
+        {
+            throw new ArgumentOutOfRangeException(nameof(target), "Only final modes can be applied live.");
+        }
+
+        ModePolicy.AssertSafe();
+        var state = await stateStore.LoadAsync(cancellationToken);
+        await system.DeleteOneShotTaskAsync(cancellationToken);
+        if (state.CurrentMode == MachineMode.RecoveryRequired)
+        {
+            return state;
+        }
+
+        var transaction = NewTransaction(state.CurrentMode, target);
+        var previousPowerPlan = state.PreviousPowerPlan ?? await system.GetActivePowerPlanAsync(cancellationToken);
+        var baseline = state.ChangedServices.Count == 0 && state.CurrentMode == MachineMode.Normal
+            ? await CaptureGamingServicesAsync(cancellationToken) : state.ChangedServices;
+        state = state with { Transaction = transaction, ChangedServices = baseline, LastError = null };
+        await stateStore.SaveAsync(state, cancellationToken);
+
+        try
+        {
+            if (target == MachineMode.Gaming)
+            {
+                await ApplyGamingAsync(cancellationToken);
+            }
+            else
+            {
+                await RestoreCapturedServicesAsync(state, cancellationToken);
+                await ApplyNonGamingPowerPlanAsync(target, state.PreviousPowerPlan, cancellationToken);
+            }
+
+            var completed = state with
+            {
+                CurrentMode = target,
+                DesiredMode = target,
+                PreviousPowerPlan = target == MachineMode.Normal ? null : previousPowerPlan,
+                ChangedServices = target == MachineMode.Normal ? [] : baseline,
+                SessionId = target == MachineMode.Normal ? null : system.CurrentSessionId,
+                Transaction = transaction with { Completed = true },
+                LastSuccessfulTransitionUtc = DateTimeOffset.UtcNow
+            };
+            await stateStore.SaveAsync(completed, cancellationToken);
+            return completed;
+        }
+        catch (Exception exception)
+        {
+            return await FailAsync(state, transaction, exception, cancellationToken);
+        }
+    }
+
+    public async Task<ModeState> NormalizeAfterBootAsync(CancellationToken cancellationToken = default)
+    {
+        var state = await stateStore.LoadAsync(cancellationToken);
+        if (state.CurrentMode is not (MachineMode.Gaming or MachineMode.Programming) ||
+            string.Equals(state.SessionId, system.CurrentSessionId, StringComparison.Ordinal))
+        {
+            return state;
+        }
+
+        return await ApplyLiveAsync(MachineMode.Normal, cancellationToken);
+    }
+
     public async Task<ModeState> ArmAsync(MachineMode target, string helperPath,
         CancellationToken cancellationToken = default)
     {
diff --git a/src/KCxWare.Core/Persistence/JsonStateStore.cs b/src/KCxWare.Core/Persistence/JsonStateStore.cs
index 389fc5c..6384129 100644
--- a/src/KCxWare.Core/Persistence/JsonStateStore.cs
+++ b/src/KCxWare.Core/Persistence/JsonStateStore.cs
@@ -28,11 +28,23 @@ public sealed class JsonStateStore : IStateStore
 
         await using var stream = File.OpenRead(_path);
         var state = await JsonSerializer.DeserializeAsync<ModeState>(stream, Options, cancellationToken);
-        if (state is null || state.SchemaVersion != ModeState.CurrentSchemaVersion)
+        if (state is null || state.SchemaVersion is < 1 or > ModeState.CurrentSchemaVersion)
         {
             return new ModeState { CurrentMode = MachineMode.RecoveryRequired, LastError = "Unsupported or invalid state file." };
         }
 
+        if (state.SchemaVersion == 1)
+        {
+            return state with
+            {
+                SchemaVersion = ModeState.CurrentSchemaVersion,
+                CurrentMode = state.CurrentMode is MachineMode.GamingArmed or MachineMode.ProgrammingArmed or MachineMode.NormalArmed
+                    ? MachineMode.RecoveryRequired : state.CurrentMode,
+                LastError = state.CurrentMode is MachineMode.GamingArmed or MachineMode.ProgrammingArmed or MachineMode.NormalArmed
+                    ? "Legacy armed transition requires safe recovery." : state.LastError
+            };
+        }
+
         if (state.Transaction is { Completed: false })
         {
             return state with { CurrentMode = MachineMode.RecoveryRequired, LastError = "An interrupted transition requires recovery." };
diff --git a/src/KCxWare.Core/Windows/WindowsSystemController.cs b/src/KCxWare.Core/Windows/WindowsSystemController.cs
index b1b05a6..e1bfe94 100644
--- a/src/KCxWare.Core/Windows/WindowsSystemController.cs
+++ b/src/KCxWare.Core/Windows/WindowsSystemController.cs
@@ -7,6 +7,7 @@ namespace KCxWare.Core.Windows;
 
 public sealed partial class WindowsSystemController(ICommandRunner runner) : ISystemController
 {
+    public string CurrentSessionId => Environment.TickCount64.ToString(System.Globalization.CultureInfo.InvariantCulture);
     public async Task<string?> GetActivePowerPlanAsync(CancellationToken cancellationToken = default)
     {
         var result = await runner.RunAsync("powercfg.exe", ["/getactivescheme"], cancellationToken);
diff --git a/src/KCxWare.Helper/Program.cs b/src/KCxWare.Helper/Program.cs
index 8f1b4f2..5731340 100644
--- a/src/KCxWare.Helper/Program.cs
+++ b/src/KCxWare.Helper/Program.cs
@@ -24,28 +24,24 @@ internal static class HelperProgram
         {
             if (args.Length == 0)
             {
-                throw new ArgumentException("Expected arm, apply-armed, recover, or cancel.");
+                throw new ArgumentException("Expected apply-live, normalize-after-boot, recover, or cancel.");
             }
 
             var orchestrator = new ModeOrchestrator(new JsonStateStore(),
                 new WindowsSystemController(new CommandRunner()), message => WriteLog(logPath, message));
             switch (args[0].ToLowerInvariant())
             {
-                case "arm" when args.Length >= 2 && Enum.TryParse<MachineMode>(args[1], true, out var mode):
-                    var helperPath = Environment.ProcessPath ?? throw new InvalidOperationException("Helper path is unavailable.");
-                    var armed = await orchestrator.ArmAsync(mode, helperPath);
-                    EnsureHealthy(armed);
-                    WriteLog(logPath, $"Armed {mode} transition.");
-                    if (args.Contains("--reboot", StringComparer.OrdinalIgnoreCase))
-                    {
-                        Restart($"KCxWare is restarting Windows into {mode} mode.");
-                    }
+                case "apply-live" when args.Length >= 2 && Enum.TryParse<MachineMode>(args[1], true, out var mode):
+                    var appliedLive = await orchestrator.ApplyLiveAsync(mode);
+                    EnsureHealthy(appliedLive);
+                    WriteLog(logPath, $"Applied {mode} mode live.");
                     break;
 
                 case "apply-armed":
-                    var applied = await orchestrator.ApplyArmedAsync();
-                    EnsureHealthy(applied);
-                    WriteLog(logPath, $"Applied {applied.CurrentMode} mode.");
+                case "normalize-after-boot":
+                    var normalized = await orchestrator.NormalizeAfterBootAsync();
+                    EnsureHealthy(normalized);
+                    WriteLog(logPath, "Normalized temporary session mode after boot.");
                     break;
 
                 case "recover":
diff --git a/src/KCxWare/MainWindow.xaml b/src/KCxWare/MainWindow.xaml
index 379c5d8..2f4c40e 100644
--- a/src/KCxWare/MainWindow.xaml
+++ b/src/KCxWare/MainWindow.xaml
@@ -94,10 +94,10 @@
 
                             <UniformGrid Columns="3" Margin="-8,20,-8,0">
                                 <Button Style="{StaticResource NeonButton}" Click="GamingClick" Margin="8" MinHeight="178" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" BorderBrush="#D620E6FF" Background="#4D06131B">
-                                    <StackPanel><TextBlock Text="GAMING MODE" FontSize="17" FontWeight="Black" Foreground="{StaticResource Cyan}" /><TextBlock Text="MAXIMUM GAME-FOCUSED PERFORMANCE" FontSize="9" Foreground="#8EDCE6" Margin="0,3,0,15" /><TextBlock Text="Restart into a lean gaming environment while preserving security, networking, audio, graphics, input, and anti-cheat." TextWrapping="Wrap" Foreground="#CBD8E3" LineHeight="19" /></StackPanel>
+                                    <StackPanel><TextBlock Text="GAMING MODE" FontSize="17" FontWeight="Black" Foreground="{StaticResource Cyan}" /><TextBlock Text="LIVE SESSION ACTIVATION" FontSize="9" Foreground="#8EDCE6" Margin="0,3,0,15" /><TextBlock Text="Apply a temporary lean gaming environment now while preserving security, networking, audio, graphics, input, and anti-cheat." TextWrapping="Wrap" Foreground="#CBD8E3" LineHeight="19" /></StackPanel>
                                 </Button>
                                 <Button Style="{StaticResource NeonButton}" Click="ProgrammingClick" Margin="8" MinHeight="178" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" BorderBrush="#D6A86DFF" Background="#4D100B1B">
-                                    <StackPanel><TextBlock Text="PROGRAMMING MODE" FontSize="17" FontWeight="Black" Foreground="#BA8DFF" /><TextBlock Text="FULL DEVELOPMENT ENVIRONMENT" FontSize="9" Foreground="#BFB0D8" Margin="0,3,0,15" /><TextBlock Text="Restore Docker, WSL, local AI, indexing, sync, and development capabilities without auto-launching heavy tools." TextWrapping="Wrap" Foreground="#CBD8E3" LineHeight="19" /></StackPanel>
+                                    <StackPanel><TextBlock Text="PROGRAMMING MODE" FontSize="17" FontWeight="Black" Foreground="#BA8DFF" /><TextBlock Text="LIVE DEVELOPMENT ACTIVATION" FontSize="9" Foreground="#BFB0D8" Margin="0,3,0,15" /><TextBlock Text="Restore development, virtualization, local-AI, indexing, and sync capabilities now without auto-launching heavy tools." TextWrapping="Wrap" Foreground="#CBD8E3" LineHeight="19" /></StackPanel>
                                 </Button>
                                 <Button Style="{StaticResource NeonButton}" Click="NormalClick" Margin="8" MinHeight="178" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" BorderBrush="#D6FF2BD6" Background="#4D190817">
                                     <StackPanel><TextBlock Text="NORMAL MODE" FontSize="17" FontWeight="Black" Foreground="{StaticResource Magenta}" /><TextBlock Text="EVERYDAY WINDOWS USE" FontSize="9" Foreground="#E79BD7" Margin="0,3,0,15" /><TextBlock Text="Balanced services, cloud sync, updates, RGB utilities, browsers, and everyday convenience features." TextWrapping="Wrap" Foreground="#CBD8E3" LineHeight="19" /></StackPanel>
@@ -119,11 +119,11 @@
                     <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Background="Transparent">
                         <StackPanel Margin="30,24" MaxWidth="900">
                             <TextBlock Text="KCxWare Guide" FontSize="28" FontWeight="Black" Foreground="{StaticResource Cyan}" />
-                            <TextBlock TextWrapping="Wrap" LineHeight="21" Margin="0,8,0,18" Foreground="#D6E2EC" Text="KCxWare switches this PC between Gaming, Programming, and Normal Everyday profiles. Use it before changing the machine's purpose; every mode transition is armed, elevated through Windows UAC, applied once after logon, and recorded under C:\ProgramData\KCxWare." />
+                            <TextBlock TextWrapping="Wrap" LineHeight="21" Margin="0,8,0,18" Foreground="#D6E2EC" Text="KCxWare switches this PC between live Gaming, Programming, and Normal profiles. Each transition is transactional, elevated through Windows UAC, verified in the current session, and recorded under C:\ProgramData\KCxWare. Gaming and Programming end at the next Windows boot, which restores Normal." />
                             <TextBlock Text="MODES AND WHEN TO USE THEM" FontWeight="Bold" Foreground="{StaticResource Magenta}" />
                             <TextBlock TextWrapping="Wrap" LineHeight="21" Margin="0,7,0,16" Text="Gaming Mode waits 30 seconds after logon for startup activity to settle, safely shuts down WSL, and then stops only KCxWare's allowlisted development, sync, indexing, RGB, and background workloads. Services must reach STOPPED before their remaining processes are terminated. Each apparently clean pass must remain clean for 35 seconds, covering WSearch's observed 30-second delayed restart action; a late WSearch restart triggers another cleanup attempt, and the existing three-attempt limit bounds the transition. Chrome, unlisted Chrome native hosts, NVIDIA Overlay, and NVIDIA Highlights/recording components are protected: they are neither stopped nor treated as cleanup failures. Edge is suppressed only when it has no visible window; shared msedgewebview2 infrastructure is never killed indiscriminately. OpenRGB and RustDesk are required cleanup targets. PhoneExperienceHost, CrossDeviceService, and CrossDeviceResume are stopped when present but are logged best-effort targets because Windows startup and COM activation may legitimately respawn them; their respawn alone does not fail Gaming Mode. Windows DCOM-owned WidgetService is excluded from failure verification for the same safety reason. A residual wslservice host process is allowed only when WSLService, vmcompute, and vmmemWSL are stopped; active services or vmmemWSL fail verification. WinFsp.Launcher is skipped when a running dependent service makes stopping it unsafe. Gaming Mode selects AMD Ryzen High Performance when installed. It never disables service startup types, recovery actions, startup tasks, COM registrations, Defender, Firewall, networking, audio, NVIDIA display essentials, Logitech input, game services, or anti-cheat. Programming Mode restores KCxWare-stopped services and prefers an installed high-performance/development plan without launching every tool. Normal Mode restores the captured pre-Gaming power plan when installed, otherwise AMD Ryzen Balanced then Windows Balanced." />
                             <TextBlock Text="SETUP, DEFAULTS, AND PERMISSIONS" FontWeight="Bold" Foreground="{StaticResource Cyan}" />
-                            <TextBlock TextWrapping="Wrap" LineHeight="21" Margin="0,7,0,16" Text="Install with installer\Install-KCxWare.ps1 or run the published folder. The UI stays unelevated. Choosing a mode shows an exact reboot confirmation, then Windows UAC elevates only KCxWare.Helper. Arming creates the elevated KCxWare Apply Armed Mode one-shot logon task. At logon, apply-armed performs the delay, cleanup, and sustained verification inline inside the helper; it is not a separate child worker. The task is removed after success, so it is normally visible only between arming and successful application. Default state is Normal and unknown or missing services are ignored. No secrets are stored. State and transaction recovery data are stored in ProgramData; helper logs contain operation names and errors only." />
+                            <TextBlock TextWrapping="Wrap" LineHeight="21" Margin="0,7,0,16" Text="Install with installer\Install-KCxWare.ps1 or run the published folder. The UI stays unelevated. Choosing a mode confirms the live change, then Windows UAC elevates only KCxWare.Helper. Live transitions capture a baseline, apply policy, verify, and commit without requesting reboot. A boot/session marker detects a later Windows boot and restores Normal through the helper. Legacy Apply Armed Mode tasks are normalized safely and cannot reapply an old temporary mode. Default state is Normal and unknown or missing services are ignored. No secrets are stored. State and transaction recovery data are stored in ProgramData; helper logs contain operation names and errors only." />
                             <TextBlock Text="APPEARANCE AND READABILITY" FontWeight="Bold" Foreground="{StaticResource Magenta}" />
                             <TextBlock TextWrapping="Wrap" LineHeight="21" Margin="0,7,0,16" Text="KCxWare loads its packaged Assets\1.png artwork locally and places translucent glass controls above it; no network access or extra permission is required. The full artwork is centered without changing its aspect ratio, while localized dark surfaces preserve readable status and Guide text. Resize the window normally when you need more room. If the artwork is missing or the background is plain black, repair or reinstall KCxWare and confirm Assets\1.png exists beside the installed application; do not substitute an online or absolute-path image." />
                             <TextBlock Text="RISKS AND RECOVERY" FontWeight="Bold" Foreground="{StaticResource Magenta}" />
diff --git a/src/KCxWare/MainWindow.xaml.cs b/src/KCxWare/MainWindow.xaml.cs
index 1199185..0425243 100644
--- a/src/KCxWare/MainWindow.xaml.cs
+++ b/src/KCxWare/MainWindow.xaml.cs
@@ -15,31 +15,53 @@ public partial class MainWindow
     {
         InitializeComponent();
         DataContext = _viewModel;
-        Loaded += async (_, _) => await _viewModel.RefreshAsync();
+        Loaded += async (_, _) =>
+        {
+            await _viewModel.RefreshAsync();
+            if (_viewModel.NeedsBootNormalization)
+            {
+                await _viewModel.NormalizeAfterBootAsync();
+                await _viewModel.RefreshAsync();
+            }
+        };
     }
 
-    private void GamingClick(object sender, RoutedEventArgs e) => ConfirmAndRestart(MachineMode.Gaming);
-    private void ProgrammingClick(object sender, RoutedEventArgs e) => ConfirmAndRestart(MachineMode.Programming);
-    private void NormalClick(object sender, RoutedEventArgs e) => ConfirmAndRestart(MachineMode.Normal);
+    private void GamingClick(object sender, RoutedEventArgs e) => ConfirmAndApply(MachineMode.Gaming);
+    private void ProgrammingClick(object sender, RoutedEventArgs e) => ConfirmAndApply(MachineMode.Programming);
+    private void NormalClick(object sender, RoutedEventArgs e) => ConfirmAndApply(MachineMode.Normal);
 
-    private void ConfirmAndRestart(MachineMode mode)
+    private void ConfirmAndApply(MachineMode mode)
     {
         var details = mode switch
         {
-            MachineMode.Gaming => "Windows will restart into a lean gaming session. Supported development, sync, indexing, overlay, and background workloads will be stopped for the session. Security, networking, audio, display, input, game services, and anti-cheat remain protected.",
-            MachineMode.Programming => "Windows will restart into Programming Mode. Services previously stopped by KCxWare will be restored and a development-oriented installed power plan selected. Heavy tools are not launched automatically.",
-            _ => "Windows will restart into Normal Mode. KCxWare-restored services and normal balanced power behavior will return; heavy development tools are not launched automatically."
+            MachineMode.Gaming => "KCxWare will apply a temporary Gaming session now. Supported development, sync, indexing, overlay, and background workloads will be stopped. Security, networking, audio, display, input, game services, and anti-cheat remain protected.",
+            MachineMode.Programming => "KCxWare will apply Programming Mode now. Services previously stopped by KCxWare will be restored and a development-oriented installed power plan selected. Heavy tools are not launched automatically.",
+            _ => "KCxWare will restore Normal Mode now. KCxWare-recorded baseline services and power behavior will return; heavy development tools are not launched automatically."
         };
 
-        var answer = MessageBox.Show($"{details}\n\nUnsaved work in other applications may be lost. Restart now?",
-            $"Restart to {mode} Mode", MessageBoxButton.YesNo, MessageBoxImage.Warning, MessageBoxResult.No);
+        var answer = MessageBox.Show($"{details}\n\nApply now?",
+            $"Apply {mode} Mode", MessageBoxButton.YesNo, MessageBoxImage.Warning, MessageBoxResult.No);
         if (answer != MessageBoxResult.Yes) return;
-        RunHelper($"arm {mode} --reboot", true);
+        RunLiveHelper(mode);
     }
 
     private async void RecoverClick(object sender, RoutedEventArgs e) => await RunRecoveryAsync();
     private void CancelClick(object sender, RoutedEventArgs e) => RunHelper("cancel", false);
 
+    private async void RunLiveHelper(MachineMode mode)
+    {
+        try
+        {
+            await _viewModel.RunElevatedAsync($"apply-live {mode}");
+            await _viewModel.RefreshAsync();
+        }
+        catch (Exception exception)
+        {
+            MessageBox.Show(exception.Message, "KCxWare", MessageBoxButton.OK, MessageBoxImage.Error);
+            await _viewModel.RefreshAsync();
+        }
+    }
+
     private async Task RunRecoveryAsync()
     {
         try
diff --git a/src/KCxWare/ViewModels/MainViewModel.cs b/src/KCxWare/ViewModels/MainViewModel.cs
index da43612..fa7b502 100644
--- a/src/KCxWare/ViewModels/MainViewModel.cs
+++ b/src/KCxWare/ViewModels/MainViewModel.cs
@@ -29,6 +29,10 @@ public sealed class MainViewModel : INotifyPropertyChanged
     public bool ShowGamingExit => _state.CurrentMode is MachineMode.Gaming or MachineMode.GamingArmed;
     public bool ShowRecovery => _state.CurrentMode == MachineMode.RecoveryRequired;
     public bool ShowCancel => _state.RebootRequired;
+    public bool NeedsBootNormalization => _state.CurrentMode is MachineMode.Gaming or MachineMode.Programming &&
+        !string.Equals(_state.SessionId, _orchestrator.CurrentSessionId, StringComparison.Ordinal);
+
+    public async Task NormalizeAfterBootAsync() => await RunElevatedAsync("normalize-after-boot");
 
     public async Task RefreshAsync()
     {
diff --git a/tests/KCxWare.Tests/Fakes.cs b/tests/KCxWare.Tests/Fakes.cs
index cfe7cf8..2fc5de1 100644
--- a/tests/KCxWare.Tests/Fakes.cs
+++ b/tests/KCxWare.Tests/Fakes.cs
@@ -52,6 +52,7 @@ internal sealed class SequencedRunner(params CommandResult[] results) : ICommand
 
 internal sealed class FakeSystem : ISystemController
 {
+    public string CurrentSessionId { get; set; } = "test-session";
     public Dictionary<string, bool> Services { get; } = new(StringComparer.OrdinalIgnoreCase);
     public Dictionary<string, int> ServiceRestartsRemaining { get; } = new(StringComparer.OrdinalIgnoreCase);
     public Dictionary<string, bool> ServiceCanStopSafely { get; } = new(StringComparer.OrdinalIgnoreCase);
diff --git a/tests/KCxWare.Tests/ModeOrchestratorTests.cs b/tests/KCxWare.Tests/ModeOrchestratorTests.cs
index f980b61..c8a8803 100644
--- a/tests/KCxWare.Tests/ModeOrchestratorTests.cs
+++ b/tests/KCxWare.Tests/ModeOrchestratorTests.cs
@@ -526,6 +526,51 @@ public sealed class ModeOrchestratorTests
         Assert.Equal(1, system.TaskDeleteCount);
     }
 
+    [Fact]
+    public async Task LiveModes_CommitWithoutArmingOrReboot()
+    {
+        var store = new MemoryStateStore();
+        var system = SystemWithPlans();
+        var orchestrator = new ModeOrchestrator(store, system);
+
+        var gaming = await orchestrator.ApplyLiveAsync(MachineMode.Gaming);
+        Assert.Equal(MachineMode.Gaming, gaming.CurrentMode);
+        Assert.False(gaming.RebootRequired);
+        Assert.False(system.TaskPresent);
+
+        var programming = await orchestrator.ApplyLiveAsync(MachineMode.Programming);
+        Assert.Equal(MachineMode.Programming, programming.CurrentMode);
+        Assert.False(programming.RebootRequired);
+
+        var normal = await orchestrator.ApplyLiveAsync(MachineMode.Normal);
+        Assert.Equal(MachineMode.Normal, normal.CurrentMode);
+        Assert.False(normal.RebootRequired);
+    }
+
+    [Fact]
+    public async Task NewSession_NormalizesTemporaryModeToNormal()
+    {
+        var initial = new ModeState
+        {
+            CurrentMode = MachineMode.Gaming,
+            DesiredMode = MachineMode.Gaming,
+            SessionId = "old-session",
+            PreviousPowerPlan = "baseline",
+            ChangedServices = [new ServiceSnapshot("WSearch", true)]
+        };
+        var store = new MemoryStateStore(initial);
+        var system = SystemWithPlans();
+        system.CurrentSessionId = "new-session";
+        system.Services["WSearch"] = false;
+        system.Plans.Add("baseline");
+
+        var result = await new ModeOrchestrator(store, system).NormalizeAfterBootAsync();
+
+        Assert.Equal(MachineMode.Normal, result.CurrentMode);
+        Assert.Contains("WSearch", system.StartedServices);
+        Assert.Equal("baseline", system.ActivePlan);
+    }
+
     private static FakeSystem SystemWithPlans()
     {
         var system = new FakeSystem();
